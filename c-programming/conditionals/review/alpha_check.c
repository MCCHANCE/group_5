#include <stdio.h>

int main()
{
	char input;
	//TODO
	//complete this part to make our code functional.


	//TODO
	//redo this part using ascil values.

	if (input >= 'a' & input <= 'z' || input >= 'A' & input <= 'Z')
		printf("%c is an alphabelt", input);
	else
		printf("%c is NOT an alphabelt", input);
}
